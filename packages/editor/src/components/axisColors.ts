/**
 * Màu 3 trục, dùng chung cho: gizmo góc màn hình, marker gốc toạ độ và 2 trục
 * chính của lưới sàn — cùng một trục thì mọi nơi phải cùng một màu.
 */
export const AXIS_X = '#ff5470'; // đỏ
export const AXIS_Y = '#8bd450'; // xanh lá
export const AXIS_Z = '#4b86c9'; // xanh dương

/**
 * Bản sậm của màu trục (cùng hue, tối ~45%), dùng cho 2 đường trục trên lưới sàn.
 * Đường sàn dài hết khung nhìn nên để màu tươi như gizmo là chiếm hết mắt; gizmo
 * và marker tâm thì nhỏ, cần tươi mới thấy.
 */
export const AXIS_X_DIM = '#8c2e3e';
export const AXIS_Y_DIM = '#4c752c';
